///
/// HybridLottieViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridLottieViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::lottienitro::views {

  extern const char HybridLottieViewComponentName[] = "LottieView";

  HybridLottieViewProps::HybridLottieViewProps(const react::PropsParserContext& context,
                                               const HybridLottieViewProps& sourceProps,
                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    resizeMode([&]() -> CachedProp<std::optional<ResizeMode>> {
      try {
        const react::RawValue* rawValue = rawProps.at("resizeMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.resizeMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<ResizeMode>>::fromRawValue(*runtime, value, sourceProps.resizeMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.resizeMode: ") + exc.what());
      }
    }()),
    renderMode([&]() -> CachedProp<std::optional<RenderMode>> {
      try {
        const react::RawValue* rawValue = rawProps.at("renderMode", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.renderMode;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<RenderMode>>::fromRawValue(*runtime, value, sourceProps.renderMode);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.renderMode: ") + exc.what());
      }
    }()),
    sourceName([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceName", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceName;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.sourceName);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.sourceName: ") + exc.what());
      }
    }()),
    sourceJson([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceJson", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceJson;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.sourceJson);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.sourceJson: ") + exc.what());
      }
    }()),
    sourceURL([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceURL", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceURL;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.sourceURL);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.sourceURL: ") + exc.what());
      }
    }()),
    sourceDotLottieURI([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceDotLottieURI", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceDotLottieURI;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.sourceDotLottieURI);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.sourceDotLottieURI: ") + exc.what());
      }
    }()),
    imageAssetsFolder([&]() -> CachedProp<std::optional<std::string>> {
      try {
        const react::RawValue* rawValue = rawProps.at("imageAssetsFolder", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.imageAssetsFolder;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::string>>::fromRawValue(*runtime, value, sourceProps.imageAssetsFolder);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.imageAssetsFolder: ") + exc.what());
      }
    }()),
    progress([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("progress", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.progress;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.progress);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.progress: ") + exc.what());
      }
    }()),
    speed([&]() -> CachedProp<std::optional<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("speed", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.speed;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<double>>::fromRawValue(*runtime, value, sourceProps.speed);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.speed: ") + exc.what());
      }
    }()),
    loop([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("loop", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.loop;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.loop);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.loop: ") + exc.what());
      }
    }()),
    autoPlay([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("autoPlay", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.autoPlay;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.autoPlay);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.autoPlay: ") + exc.what());
      }
    }()),
    enableMergePathsAndroidForKitKatAndAbove([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableMergePathsAndroidForKitKatAndAbove", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableMergePathsAndroidForKitKatAndAbove;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.enableMergePathsAndroidForKitKatAndAbove);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.enableMergePathsAndroidForKitKatAndAbove: ") + exc.what());
      }
    }()),
    applyOpacityToLayersAndroid([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("applyOpacityToLayersAndroid", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.applyOpacityToLayersAndroid;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.applyOpacityToLayersAndroid);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.applyOpacityToLayersAndroid: ") + exc.what());
      }
    }()),
    enableSafeModeAndroid([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("enableSafeModeAndroid", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enableSafeModeAndroid;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.enableSafeModeAndroid);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.enableSafeModeAndroid: ") + exc.what());
      }
    }()),
    hardwareAccelerationAndroid([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hardwareAccelerationAndroid", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hardwareAccelerationAndroid;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.hardwareAccelerationAndroid);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.hardwareAccelerationAndroid: ") + exc.what());
      }
    }()),
    cacheComposition([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("cacheComposition", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.cacheComposition;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.cacheComposition);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.cacheComposition: ") + exc.what());
      }
    }()),
    colorFilters([&]() -> CachedProp<std::optional<std::vector<LottieColorFilter>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("colorFilters", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.colorFilters;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<LottieColorFilter>>>::fromRawValue(*runtime, value, sourceProps.colorFilters);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.colorFilters: ") + exc.what());
      }
    }()),
    textFiltersAndroid([&]() -> CachedProp<std::optional<std::vector<TextFilterAndroid>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("textFiltersAndroid", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.textFiltersAndroid;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<TextFilterAndroid>>>::fromRawValue(*runtime, value, sourceProps.textFiltersAndroid);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.textFiltersAndroid: ") + exc.what());
      }
    }()),
    textFiltersIOS([&]() -> CachedProp<std::optional<std::vector<TextFilterIOS>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("textFiltersIOS", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.textFiltersIOS;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<TextFilterIOS>>>::fromRawValue(*runtime, value, sourceProps.textFiltersIOS);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.textFiltersIOS: ") + exc.what());
      }
    }()),
    onAnimationFinish([&]() -> CachedProp<std::optional<std::function<void(bool /* isCancelled */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAnimationFinish", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAnimationFinish;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(bool /* isCancelled */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAnimationFinish);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.onAnimationFinish: ") + exc.what());
      }
    }()),
    onAnimationFailure([&]() -> CachedProp<std::optional<std::function<void(const std::string& /* error */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAnimationFailure", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAnimationFailure;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::string& /* error */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAnimationFailure);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.onAnimationFailure: ") + exc.what());
      }
    }()),
    onAnimationLoaded([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onAnimationLoaded", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onAnimationLoaded;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onAnimationLoaded);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.onAnimationLoaded: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridLottieViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridLottieViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("LottieView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridLottieViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("resizeMode"): return true;
      case hashString("renderMode"): return true;
      case hashString("sourceName"): return true;
      case hashString("sourceJson"): return true;
      case hashString("sourceURL"): return true;
      case hashString("sourceDotLottieURI"): return true;
      case hashString("imageAssetsFolder"): return true;
      case hashString("progress"): return true;
      case hashString("speed"): return true;
      case hashString("loop"): return true;
      case hashString("autoPlay"): return true;
      case hashString("enableMergePathsAndroidForKitKatAndAbove"): return true;
      case hashString("applyOpacityToLayersAndroid"): return true;
      case hashString("enableSafeModeAndroid"): return true;
      case hashString("hardwareAccelerationAndroid"): return true;
      case hashString("cacheComposition"): return true;
      case hashString("colorFilters"): return true;
      case hashString("textFiltersAndroid"): return true;
      case hashString("textFiltersIOS"): return true;
      case hashString("onAnimationFinish"): return true;
      case hashString("onAnimationFailure"): return true;
      case hashString("onAnimationLoaded"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridLottieViewComponentDescriptor::HybridLottieViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridLottieViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                      const std::shared_ptr<const react::Props>& props,
                                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridLottieViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridLottieViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridLottieViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridLottieViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridLottieViewProps>& props = std::const_pointer_cast<HybridLottieViewProps>(constProps);
    HybridLottieViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::lottienitro::views
